{
  "name": "grand-strand-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for secure private file access via R2",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
